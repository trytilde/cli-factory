type: object
properties:
  messages:
    type: array
    description: Matching Gmail message summaries.
    items:
      type: object
  result_size_estimate:
    type: integer
    description: Gmail estimate of total matching results.
  next_page_token:
    type: string
    description: Token for the next Gmail result page when available.
