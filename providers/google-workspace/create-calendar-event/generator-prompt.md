# Create Calendar Event Tool Generation Guidance

Use Google Calendar `events.insert`. Preserve dry-run behavior. Prefer
`calendar.events`-level scopes rather than broader Calendar scopes when possible.
