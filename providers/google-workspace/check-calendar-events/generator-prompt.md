# Check Calendar Events Tool Generation Guidance

Use Google Calendar `events.list`. Default to `primary`, a small max result
count, `singleEvents=true`, and chronological ordering for agent-readable
schedule checks.
