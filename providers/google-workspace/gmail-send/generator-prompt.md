# Gmail Send Tool Guidance

Send a plain-text Gmail message using users.messages.send. Build a MIME message,
base64url encode it, and return Gmail identifiers. Do not expose arbitrary raw
Gmail API request fields.
