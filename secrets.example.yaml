# Sensitive local/dev secrets. Copy to secrets.yaml, populate values, then run:
#   make sops-encrypt
#
# Decrypt committed secrets.enc.yaml with:
#   make sops-decrypt

openai_api_key: sk-proj-replace-me

